# Patient Microservice

Spring Boot microservice for patient profile APIs used by the frontend.

## Port

- `8089`

## Base URL

- `/api/patients`

## Endpoints

- `GET /api/patients/profile`
- `PUT /api/patients/profile`
- `GET /api/patients/{id}`

All endpoints require `Authorization: Bearer <token>`.
